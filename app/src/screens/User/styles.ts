import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  interestList: {
    flexDirection: 'row',
    flexWrap: 'wrap',
    marginHorizontal: -6,
  },

  header: {
    paddingBottom: 12,
    margin: -16,
    marginBottom: 16,
    justifyContent: 'center',
    alignItems: 'center',
  },
})
